<!DOCTYPE html>
    <html lang="en">
        <head>
            <meta charset="UTF-8">
            <meta name="viewport" content="width=device-width, initial-scale=1.0">
            <title>BẢNG BÁO CÁO</title>
            <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css">
            <link rel="stylesheet" href="/Dan/css/report.css">
            <link rel="icon" href="/Dan/images/icon9.ico" type="image/x-icon">

            {{!-- Recaptcha --}}
            <!-- Recaptcha -->
            <script src="https://www.google.com/recaptcha/enterprise.js" async defer></script>
            {{!-- summernote --}}
            <link href="https://stackpath.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css" rel="stylesheet">
            <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
            <script src="https://stackpath.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>


            <script>

                function validateRecaptcha() {
                    var response = grecaptcha.getResponse();
                    console.log('Aaaa:', response);

                    if (response.length == 0) {
                        alert('Vui lòng xác nhận bạn không phải robot.');
                        return false;
                    }
                    return true;
                }

                var alertMsg = '{{session.alertMsg}}';

                // Hiển thị thông báo nếu có
                if (alertMsg) {
                    alert(alertMsg);
                    setTimeout(function() {
            
                        alertMsg = null;
                    }, 5000);
                }
            </script>
        </head>
        <body>

            {{{body}}}


            <link href="https://cdn.jsdelivr.net/npm/summernote@0.8.18/dist/summernote.min.css" rel="stylesheet">
            <script src="https://cdn.jsdelivr.net/npm/summernote@0.8.18/dist/summernote.min.js"></script>
            <script>
                $('#summernote').summernote({
                    placeholder: 'Hello Bootstrap 4',
                    tabsize: 2,
                    height: 100
                });

                var alertErr = new URLSearchParams(window.location.search).get('alertErr');

                // Hiển thị thông báo nếu có
                if (alertErr) {
                    alert(alertErr);
                }
            </script>

        </body>
    </html>
